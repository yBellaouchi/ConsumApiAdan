import { DeleteDigitsPipePipe } from './delete-digits-pipe.pipe';

describe('DeleteDigitsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DeleteDigitsPipePipe();
    expect(pipe).toBeTruthy();
  });
});

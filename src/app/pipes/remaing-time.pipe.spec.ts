import { RemaingTimePipe } from './remaing-time.pipe';

describe('RemaingTimePipe', () => {
  it('create an instance', () => {
    const pipe = new RemaingTimePipe();
    expect(pipe).toBeTruthy();
  });
});
